using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GUI_Buttons : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    private void OnGUI()
    {
        //GUI.Button(new Rect(Screen.width/2 - 200, Screen.height/2 + 10, 100, 80), "Map it");
        //GUI.Button(new Rect(Screen.width/2 + 100, Screen.height / 2 + 10, 100, 80), "Reality check");
    }
}
